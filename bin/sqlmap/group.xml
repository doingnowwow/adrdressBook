<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
			PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
			"http://ibatis.apache.org/dtd/sql-map-2.dtd">
			
<!-- namespace속성 : 아래에서 작성한 SQL을 호출할 때 같이 사용된다. -->
<sqlMap namespace="group">
	<!-- 
		이 영역에서 sql문에 맞는 태그를 사용하여 sql문을 작성한다.
		
		사용할 수 있는 기본적인 태그들
		
		<select>~~~</select>
		<insert>~~~</insert>
		<update>~~~</update>
		<delete>~~~</delete> 
		
		위 태그에서 사용되는 속성들
			1) id속성 : 해당 태그를 호출할 때 <sqlMap>태그의 namespace와
						연결하여 사용하는 이름
			2) parameterClass속성 : sql문에 사용될 데이터가 들어있는
									 객체를 지정한다.
								(보통 VO클래스, 자바의 자료형이름 등이 사용된다.)
								(typeAlias로 지정한 alias명을 사용할 수 있다.)
			3) resultClass속성 : select문을 실행한 결과를 담을 객체를 지정한다.
								(보통 VO클래스, 자바의 자료형이름 등이 사용된다.)
	-->
	
	
	<!--전체 그룹 조회하기  -->
	<select id="selectGroupList" resultClass="GroupVO">
		SELECT * FROM TB_GROUP
	</select>

	<!--그룹 추가하기 -->
	<insert id="insertGroup" parameterClass="String">
		INSERT INTO TB_GROUP
		(GROUP_NO, GROUP_NAME)
		VALUES(GROUP_SEQ.NEXTVAL,#group_name#)
	</insert>

	<!--그룹 삭제하기 -->
	<delete id="deleteGroup" parameterClass="int">
		DELETE FROM TB_GROUP
		WHERE GROUP_NO = #group_no#
	</delete>

	<!--그룹 이름 변경하기 -->
	<update id="updateGroup" parameterClass="GroupVO">
		UPDATE TB_GROUP
		SET GROUP_NAME = #group_name#
		WHERE GROUP_NO = #group_no#
	</update>
	
</sqlMap>





